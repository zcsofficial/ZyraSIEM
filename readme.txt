pip install pymongo requests psutil uuid dnspython scapy ipinfo

pip install pywin32

pip install psutil pymongo scapy ipinfo pywin32 requests pillow
pip install psutil pymongo scapy ipinfo pywin32 requests pillow virustotal-python wget.